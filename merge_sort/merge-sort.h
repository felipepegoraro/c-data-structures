#ifndef MERGE_SORT_H

#include <iostream>
#include <vector>
#include <cassert>
void display(int *, size_t);
void merge(int *, int, int, int);
void merge_sort(int *, int l, int r);

#endif // !MERGE_SORT_H
